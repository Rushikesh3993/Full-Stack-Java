#include <stdio.h>
#include <conio.h>

void main(){
    int a = 10, b = 20;
    if(a > b){
        printf("a is largest Number: %d", a);
    }else{
        printf("b is largest Number: %d", b);
    }
}